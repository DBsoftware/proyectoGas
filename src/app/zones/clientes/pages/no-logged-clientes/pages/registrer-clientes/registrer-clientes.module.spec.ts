import { RegistrerClientesModule } from './registrer-clientes.module';

describe('RegistrerModule', () => {
  let registrerModule: RegistrerClientesModule;

  beforeEach(() => {
    // registrerModule = new registrerModule();
  });

  it('should create an instance', () => {
    expect(registrerModule).toBeTruthy();
  });
});
